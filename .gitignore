# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Jupyter
*.nbconvert.ipynb

# Data too large or not redistributable (keep small source files tracked)
data/raw/*.zip
data/raw/*.gpkg
data/raw/*.tif
data/processed/*.parquet
# BRO head series: ~2.5 GB of CSV, regenerable with
# scripts/fetch_bro_grondwater.py --series. The index stays tracked.
data/raw/APIDatacollection/series/GLD*.csv

# OS / Drive
.DS_Store
Icon?
*.gdoc
*.gsheet
